
/*
 * atoi cover a string (number) to int
 */
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	int 	i;
	char	*buf = "15";

	i = atoi(buf); 
	fprintf(stdout, "i: %d(%s)\n", i, buf);

	return 0;
}
